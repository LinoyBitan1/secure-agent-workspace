apiVersion: saw.redhat.com/v1alpha1
kind: Providers
metadata:
  profile: integrations
spec:
  providers:
    - name: mail-write
      type: custom
      credentialSecret: mail-write-token
      credentialSecretKey: api_key
