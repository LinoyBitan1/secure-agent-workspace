apiVersion: saw.redhat.com/v1alpha1
kind: Providers
metadata:
  profile: integrations
spec:
  providers:
    - name: slack-bot
      type: custom
      credentialSecret: slack-bot-token
      credentialSecretKey: bot_token
