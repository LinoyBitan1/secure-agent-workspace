apiVersion: saw.redhat.com/v1alpha1
kind: Providers
metadata:
  profile: integrations
spec:
  providers:
    - name: slack-write
      type: custom
      credentialSecret: slack-write-token
      credentialSecretKey: api_key
